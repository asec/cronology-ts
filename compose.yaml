name: cronology
services:
    api:
        image: asector/cronology:api-2.0.0
        ports:
            -   7331:7331
        volumes:
            -   ${CONF_API_HTTPS_PRIVATEKEY}:/etc/ssl/certs/cronology.key
            -   ${CONF_API_HTTPS_CERTIFICATE}:/etc/ssl/certs/cronology.crt